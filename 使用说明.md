# 本地英语录音学习工具

## 免安装网页版

访问 https://tylerzhang130666.github.io/local-english-audio-learning/ ，选择本机录音后即可使用。网页版通过 Transformers.js 在当前浏览器运行 whisper-tiny.en，录音不会上传服务器；模型首次使用需要联网下载，逐字稿和练习记录保存在浏览器中。网页版适合快速体验和较短录音。长录音、更高识别准确率或离线稳定使用仍建议采用下方的 Windows 本地版。

1. 将手机录音复制到本项目的 `audio` 文件夹（支持 m4a、mp3、wav 等）。
2. 双击 `start.bat`，浏览器会打开 http://127.0.0.1:8765 。保持启动窗口开启。
3. 点击「刷新」，选择录音，再点「开始英文转录」。CPU 转录较慢时请等待，网页会显示进度。
4. 点击句子时间戳跳转播放。可调节速度、前后跳转 5 秒；播放中的句子有绿色边框。
5. 直接修改英文文本，点击收藏或待学习标记，填写笔记，再点「保存修改」。切换录音时会先保存；关闭页面前若未保存会提醒。
6. 可筛选收藏、待学习句子，也可搜索文本或笔记。导出始终包含全部句子；Markdown 和 TXT 包括学习标记和笔记，SRT 仅包括修改后的字幕。
7. 使用完毕，在启动窗口按 Ctrl+C 停止服务。

## 发音与听力重点

每句话可展开「发音 + 听力重点」。系统会根据句子结构给出 2–4 个建议重读词、常见弱读词和可能连读位置；这些是学习提示，不是对录音重音的声学测量，可以手动修改。点击英文单词可离线查询美式 IPA、音节与主重音，点击音标查看中文发音提示，并可调用浏览器的系统美式语音。

单词面板支持录下自己的读音并回听。录音保存在当前浏览器的本地数据库，不上传；更换浏览器或清理网站数据会丢失，因此重要练习需自行保留。可以为具体单词标记 `Pronunciation mismatch`，也可填写中文含义和 `My Response`。本功能不自动判断音位是否准确，也不生成发音分数；请把录音对比和标记作为练习工具。

IPA 数据来自 Carnegie Mellon University 的 CMU Pronouncing Dictionary（美式英语）。专业缩写、人名及未收录词会显示“未收录”，需先核对拼写，不会自动猜测音标。

## 数据与备份

原始音频存放于 `audio`，逐字稿、原始识别文本、词级时间戳、标记和笔记保存在 `data` 中的 JSON 文件。备份时一起复制这两个文件夹。录音文件名用于关联逐字稿，因此开始学习后请勿改名。保存采用临时文件替换，且检测多页面编辑冲突。发生冲突时可先导出当前页面内容，再刷新。

界面按带时间戳的语音片段显示，不保证每一片段严格对应一个语法句子。词级时间戳保留在 JSON 中；修改文本不会重新对齐时间戳。自动识别可能出错，尤其是人名、专业词汇或多人重叠说话，需结合录音校正。本版不区分说话人。

## 初次安装或迁移电脑

需有 Python 3.11–3.13 和 pip。双击 `setup.bat` 安装项目独立环境和 faster-whisper，并准备 `small.en` 英文模型。仅安装依赖和首次缺少模型时需要联网；正常使用时模型仅从本地加载，不会上传录音，也不调用付费 API。模型缓存通常位于用户目录 `.cache/huggingface/hub`。迁移电脑时需重新安装或迁移模型缓存。

网页服务只监听 127.0.0.1。页面没有外部字体、脚本或统计服务。不要把服务改成公网监听。音频解码由 faster-whisper 的 PyAV 处理；通常无需独立安装 FFmpeg。浏览器无法播放某些音频编码时，可将其转为 MP3/WAV 后重新加入。

若 8765 端口被占用，在项目目录运行 `.venv\Scripts\python.exe app.py --port 8766`。如果缺少依赖或模型，重新运行 `setup.bat`。关闭程序会中止正在进行的转录，下一次可重新开始；已有完成的逐字稿不会被覆盖。

技术参考：https://github.com/SYSTRAN/faster-whisper

## 已验证的样例

附带的 `audio/example_jfk.wav` 是 whisper.cpp 官方仓库的公开英文测试录音（约 11 秒），来源：https://github.com/ggml-org/whisper.cpp/blob/master/samples/jfk.wav 。它不是你的会议录音。已用本机 CPU / int8 模式完成转录，并验证逐句播放、变速、笔记和标记保存。可以先选择该样例体验功能。

后端测试：在本目录运行 `.venv\Scripts\python.exe -m unittest test_app -v`，涵盖音频 Range 请求、保存与冲突检测、本机访问与文件路径限制。
